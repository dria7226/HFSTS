DATA_TYPE read_array(DATA_TYPE array_index, DATA_TYPE buffer_index)
{
  //FILE* f = fopen(memory[SOURCE_AT(head_index)].data[buffer_index],"rb");

  //if(f == NULL) return;

  unsigned char i = 0;
  //while((program[i++] = (unsigned char)fgetc(f)) == EOF);

  //fclose(f);
}

DATA_TYPE write_array(DATA_TYPE array_index, DATA_TYPE buffer_index)
{
  //FILE* f = fopen(memory[SOURCE_AT(head_index)].data[buffer_index],"wb");

  //if(f == NULL) return;

  unsigned char i;
  for(i = 0; i < 2; i++)
  {
    //fputc(test_program[i],f);
  }

  //fclose(f);
}
